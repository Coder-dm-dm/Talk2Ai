version: "3.8"
services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - GOOGLE_GENAI_API_KEY=
      - PROMPT_FILE=/data/prompt_store.json
      - APP_LOG_FILE=/data/app.log
    volumes:
      - ./data:/data
